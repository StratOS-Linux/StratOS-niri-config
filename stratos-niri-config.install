post_install() {
  echo "==> Niri configuration has been installed to /etc/skel/.config/niri"
  echo "==> To use it, copy it to your home directory:"
  echo "    cp -r /etc/skel/.config/niri ~/.config/"
}